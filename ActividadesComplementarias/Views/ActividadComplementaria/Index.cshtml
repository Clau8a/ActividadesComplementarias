@model IEnumerable<ActividadesComplementarias.Models.ActividadComplementaria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-10 col-lg-offset-1">
    <h2>Actividades Complementarias</h2>
    <div id="agregar">
        <span><a href="/ActividadComplementaria/Create" class="addNew"><span class="glyphicon glyphicon-plus"></span></a> <b>  Agregar Nuevo</b></span>
    </div>
    <table class="table table-bordered table-hover">
        <tr>
            <th>
                <label>Clave Actividad</label>
            </th>
            <th>
                <label>Nombre</label>
            </th>
            <th>
                <label>Descripción</label>
            </th>
            <th>
                <label>Departamento</label>
            </th>
            <th>
                <label>Modalidad</label>
            </th>
            <th>
                <label>No. Créditos</label>
            </th>
            <th>Grupos</th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr class="success">
                <td>
                    @Html.DisplayFor(modelItem => item.idActividadComplementaria)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nombreActComplementaria)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Departamento1.nombreDepartamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.modalidad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.noCreditos)
                </td>
                <td><a href="/Grupos/Index/@item.idActividadComplementaria"><span id="edit" class="glyphicon glyphicon-list cuadrado"></span></a></td>
                <td style="width:100px;">
                    
                    <a href="/ActividadComplementaria/Edit/@item.idActividadComplementaria"><span id="edit" class="glyphicon glyphicon-pencil cuadrado"></span></a>
                    <a href="/ActividadComplementaria/Delete/@item.idActividadComplementaria"><span id="delete" class="glyphicon glyphicon-remove cuadrado"></span></a>

                </td>
            </tr>
        }

    </table>
</div>