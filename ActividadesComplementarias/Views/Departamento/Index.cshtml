@model IEnumerable<ActividadesComplementarias.Models.Departamento>

@{
    ViewBag.Title = "CAS - Departamento";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <span class="tituloh2">Departamento</span>

    <span>
        <a href="/Departamento/Create" class="addNew"><span class="glyphicon glyphicon-plus"></span></a>
    </span>
</div>

<table>
    <tr>
        <th>
            <label>Clave Departamento</label>
        </th>
        <th>
           <label>Departamento</label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idDepartamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombreDepartamento)
        </td>
        <td>
            <a href="/Departamento/Edit/@item.idDepartamento"><span class=""></span></a> |
            <a href="/Departamento/Delete/@item.idDepartamento"><span class""></span></a>
        </td>
    </tr>
}

</table>